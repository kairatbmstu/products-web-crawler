#include <iostream>
#include <string> // for string class
using namespace std;

namespace WebCrawler
{
    namespace Model
    {
        class AppUser
        {
        private:
            /* data */
        public:
            AppUser(/* args */);
            ~AppUser();
        };

        AppUser::AppUser(/* args */)
        {
        }

        AppUser::~AppUser()
        {
        }
    }
}